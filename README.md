# tx_manager
A dcc agnostic tool that lets users batch create .tx files from existing images in a directory using maketx. It also handles the conversion of those textures from srgb space into the ACES colorspace with OCIO using naming conventions or user overrides.
